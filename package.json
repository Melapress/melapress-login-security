{
  "name": "password-policy-manager",
  "version": "2.6.0",
  "description": "WordPress WPassword allows WordPress administrators to configure password policies for WordPress users to use strong passwords.",
  "main": "gulpfile.js",
  "scripts": {
    "release": "composer install --no-dev && gulp substitute-year && gulp replace-latest-version-numbers && gulp translate && gulp zip"
  },
  "author": "WP White Security",
  "license": "GPL-3.0",
  "engines": {
    "node": "^15.14.0"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-minify": "^3.1.0",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.1.3",
    "gulp-sort": "^2.0.0",
    "gulp-wp-pot": "^2.5.0",
    "gulp-zip": "^5.1.0"
  }
}
